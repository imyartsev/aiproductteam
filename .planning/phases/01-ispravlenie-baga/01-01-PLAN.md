---
phase: 1
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/po.py
  - agents/architect.py
  - tests/test_agents.py
autonomous: true
requirements:
  - FIX-01
must_haves:
  truths:
    - "В agents/po.py атрибут называется use_reasoner = True (не use_heavy_model)"
    - "В agents/architect.py атрибут называется use_reasoner = True (не use_heavy_model)"
    - "При HEAVY_MODEL=true оба агента используют deepseek-reasoner, без флага — deepseek-chat"
    - "Тесты в tests/test_agents.py проходят без ошибок"
  artifacts:
    - path: "agents/po.py"
      provides: "POAgent с исправленным атрибутом use_reasoner"
      contains: "use_reasoner = True"
    - path: "agents/architect.py"
      provides: "ArchitectAgent с исправленным атрибутом use_reasoner"
      contains: "use_reasoner = True"
    - path: "tests/test_agents.py"
      provides: "Рабочие тесты с OpenAI SDK mock и поведенческие тесты HEAVY_MODEL"
  key_links:
    - from: "agents/po.py:use_reasoner"
      to: "agents/base.py:__init__"
      via: "self.use_reasoner читается в BaseAgent.__init__ при выборе модели"
    - from: "agents/architect.py:use_reasoner"
      to: "agents/base.py:__init__"
      via: "self.use_reasoner читается в BaseAgent.__init__ при выборе модели"
---

# Plan 1-1: Исправление бага use_heavy_model → use_reasoner и починка тестов

<objective>
Переименовать атрибут `use_heavy_model` в `use_reasoner` в `agents/po.py` и `agents/architect.py` (per D-01, D-02), чтобы флаг `HEAVY_MODEL=true` корректно активировал модель `deepseek-reasoner`. Параллельно починить тесты в `tests/test_agents.py`: заменить мок Anthropic SDK на OpenAI SDK (per D-04) и добавить поведенческий тест на флаг HEAVY_MODEL (per D-05).
</objective>

<tasks>

## Task 1: Переименовать use_heavy_model → use_reasoner в po.py и architect.py

**Type:** edit
**Files:** `agents/po.py`, `agents/architect.py`

**Read first:**
- `agents/po.py` — строка 9: атрибут `use_heavy_model = True`, который нужно переименовать
- `agents/architect.py` — строка 9: атрибут `use_heavy_model = True`, который нужно переименовать
- `agents/base.py` — строки 33 и 38: убедиться, что BaseAgent ожидает именно `use_reasoner` (не `use_heavy_model`) при выборе модели в `__init__`

**Action:**

В `agents/po.py` строка 9: заменить `use_heavy_model = True` на `use_reasoner = True`.

В `agents/architect.py` строка 9: заменить `use_heavy_model = True` на `use_reasoner = True`.

Больше ничего не менять — env var `HEAVY_MODEL` остаётся без изменений (per D-03).

**Acceptance criteria:**
- [ ] В `agents/po.py` строка 9 содержит ровно `use_reasoner = True` (не `use_heavy_model`)
- [ ] В `agents/architect.py` строка 9 содержит ровно `use_reasoner = True` (не `use_heavy_model`)
- [ ] В обоих файлах строка `use_heavy_model` полностью отсутствует
- [ ] Env var `HEAVY_MODEL` в `agents/base.py` не изменён (строка 37 по-прежнему `os.environ.get("HEAVY_MODEL", "false")`)

---

## Task 2: Починить существующие тесты в test_agents.py (мок OpenAI вместо Anthropic)

**Type:** edit
**Files:** `tests/test_agents.py`

**Read first:**
- `tests/test_agents.py` — весь файл: понять три существующих теста и их текущие сломанные моки (`messages.create`, `response.content[0].text`, `cache_control: ephemeral`)
- `agents/base.py` — строки 40-51: метод `run()`, чтобы понять правильный способ мокирования (`chat.completions.create`, `response.choices[0].message.content`)

**Action:**

Переписать вспомогательную функцию `_make_mock_response` и все три теста, заменив Anthropic SDK API на OpenAI SDK API:

1. Функция `_make_mock_response(text: str)` — изменить структуру ответа:
   - Старая структура: `response.content = [block]` где `block.text = text`
   - Новая структура: `response.choices[0].message.content = text`
   - Реализация: создать `mock_message = MagicMock()`, установить `mock_message.content = text`, создать `mock_choice = MagicMock()`, установить `mock_choice.message = mock_message`, создать `response = MagicMock()`, установить `response.choices = [mock_choice]`

2. В тесте `test_base_agent_run`: заменить `mock_client.messages.create` на `mock_client.chat.completions.create` во всех трёх местах (установка return_value и assert_called_once)

3. В тесте `test_po_agent_populates_task_spec`: аналогично заменить `mock_client.messages.create` на `mock_client.chat.completions.create`

4. Тест `test_prompt_cache_control_set` — этот тест проверяет `cache_control: ephemeral`, которого нет в OpenAI SDK и в текущей реализации `base.py`. Заменить его на тест `test_base_agent_run_calls_completions_create`, который проверяет:
   - `mock_client.chat.completions.create.assert_called_once()` (метод был вызван)
   - Аргументы вызова: `call_kwargs["model"]` присутствует, `call_kwargs["messages"]` — список из двух элементов (system + user)

Строку `os.environ.setdefault("ANTHROPIC_API_KEY", "test-key")` заменить на `os.environ.setdefault("DEEPSEEK_API_KEY", "test-key")` — ключ теперь правильный.

**Acceptance criteria:**
- [ ] `os.environ.setdefault` использует `DEEPSEEK_API_KEY`, а не `ANTHROPIC_API_KEY`
- [ ] Функция `_make_mock_response` возвращает объект с `response.choices[0].message.content == text`
- [ ] В `test_base_agent_run` используется `mock_client.chat.completions.create`, а не `mock_client.messages.create`
- [ ] В `test_po_agent_populates_task_spec` используется `mock_client.chat.completions.create`, а не `mock_client.messages.create`
- [ ] Тест `test_prompt_cache_control_set` удалён или заменён на тест, не проверяющий `cache_control`
- [ ] Команда `pytest tests/test_agents.py -v` проходит без ошибок (0 failed)

---

## Task 3: Добавить поведенческие тесты флага HEAVY_MODEL

**Type:** edit
**Files:** `tests/test_agents.py`

**Read first:**
- `tests/test_agents.py` — файл после Task 2: понять текущую структуру, куда добавить новые тесты
- `agents/base.py` — строки 29-38: `BaseAgent.__init__` с логикой `heavy and self.use_reasoner → REASONER_MODEL`; константы `CHAT_MODEL = "deepseek-chat"` и `REASONER_MODEL = "deepseek-reasoner"` на строках 8-9
- `agents/po.py` — убедиться, что после Task 1 атрибут `use_reasoner = True`
- `agents/architect.py` — убедиться, что после Task 1 атрибут `use_reasoner = True`

**Action:**

Добавить в конец `tests/test_agents.py` два новых теста (per D-05). Тесты проверяют атрибут `agent._model` — он устанавливается в `__init__` до любых API-вызовов, поэтому мокирование API не нужно. Нужно только подставить `DEEPSEEK_API_KEY` и мокнуть `_load_prompt` (чтобы не читать файлы промптов с диска):

```python
@patch("agents.base._load_prompt", return_value="system prompt")
def test_po_uses_reasoner_when_heavy_model_true(mock_prompt):
    """При HEAVY_MODEL=true POAgent инициализируется с deepseek-reasoner."""
    from agents.po import POAgent
    os.environ["HEAVY_MODEL"] = "true"
    try:
        agent = POAgent()
        assert agent._model == "deepseek-reasoner"
    finally:
        del os.environ["HEAVY_MODEL"]


@patch("agents.base._load_prompt", return_value="system prompt")
def test_po_uses_chat_without_heavy_model(mock_prompt):
    """Без HEAVY_MODEL POAgent инициализируется с deepseek-chat."""
    from agents.po import POAgent
    os.environ.pop("HEAVY_MODEL", None)
    agent = POAgent()
    assert agent._model == "deepseek-chat"


@patch("agents.base._load_prompt", return_value="system prompt")
def test_architect_uses_reasoner_when_heavy_model_true(mock_prompt):
    """При HEAVY_MODEL=true ArchitectAgent инициализируется с deepseek-reasoner."""
    from agents.architect import ArchitectAgent
    os.environ["HEAVY_MODEL"] = "true"
    try:
        agent = ArchitectAgent()
        assert agent._model == "deepseek-reasoner"
    finally:
        del os.environ["HEAVY_MODEL"]


@patch("agents.base._load_prompt", return_value="system prompt")
def test_architect_uses_chat_without_heavy_model(mock_prompt):
    """Без HEAVY_MODEL ArchitectAgent инициализируется с deepseek-chat."""
    from agents.architect import ArchitectAgent
    os.environ.pop("HEAVY_MODEL", None)
    agent = ArchitectAgent()
    assert agent._model == "deepseek-chat"
```

**Acceptance criteria:**
- [ ] Добавлены 4 новых теста: `test_po_uses_reasoner_when_heavy_model_true`, `test_po_uses_chat_without_heavy_model`, `test_architect_uses_reasoner_when_heavy_model_true`, `test_architect_uses_chat_without_heavy_model`
- [ ] Каждый тест мокает `agents.base._load_prompt` через `@patch` — без реального чтения файлов промптов
- [ ] Тесты с `HEAVY_MODEL=true` используют `try/finally` для очистки env var
- [ ] Команда `pytest tests/test_agents.py -v` проходит все тесты включая 4 новых (0 failed)
- [ ] Команда `pytest tests/ -v` проходит без ошибок — тесты в `test_pipeline.py` не затронуты

</tasks>

<verification>
## Verification Steps

1. Проверить переименование атрибутов:
   ```
   grep -n "use_heavy_model" agents/po.py agents/architect.py
   ```
   Результат должен быть пустым (совпадений нет).

2. Проверить корректное имя атрибута:
   ```
   grep -n "use_reasoner" agents/po.py agents/architect.py
   ```
   Должно показать строку 9 в обоих файлах с `use_reasoner = True`.

3. Запустить тесты агентов:
   ```
   pytest tests/test_agents.py -v
   ```
   Все тесты должны пройти (0 failed). Ожидаемый список тестов:
   - `test_base_agent_run` PASSED
   - `test_po_agent_populates_task_spec` PASSED
   - `test_base_agent_run_calls_completions_create` PASSED (или аналог вместо удалённого test_prompt_cache_control_set)
   - `test_po_uses_reasoner_when_heavy_model_true` PASSED
   - `test_po_uses_chat_without_heavy_model` PASSED
   - `test_architect_uses_reasoner_when_heavy_model_true` PASSED
   - `test_architect_uses_chat_without_heavy_model` PASSED

4. Убедиться, что pipeline-тесты не сломались:
   ```
   pytest tests/test_pipeline.py -v
   ```
   Все тесты должны пройти (0 failed).
</verification>

<success_criteria>
## Success Criteria (must_haves)

- [ ] В `agents/po.py` строка 9: атрибут называется `use_reasoner = True` (не `use_heavy_model`)
- [ ] В `agents/architect.py` строка 9: атрибут называется `use_reasoner = True` (не `use_heavy_model`)
- [ ] При `HEAVY_MODEL=true` оба агента (`POAgent`, `ArchitectAgent`) используют модель `deepseek-reasoner` (проверено тестами `test_po_uses_reasoner_when_heavy_model_true` и `test_architect_uses_reasoner_when_heavy_model_true`)
- [ ] Без флага `HEAVY_MODEL` оба агента используют `deepseek-chat` (проверено тестами `test_po_uses_chat_without_heavy_model` и `test_architect_uses_chat_without_heavy_model`)
- [ ] `pytest tests/test_agents.py -v` — 0 failed
- [ ] `pytest tests/test_pipeline.py -v` — 0 failed (не затронуты)
</success_criteria>

<threat_model>
## Threat Model

**Scope:** Этап 1 — переименование атрибута в двух файлах и правка тестов

### Trust Boundaries

| Граница | Описание |
|---------|----------|
| env var → BaseAgent | `HEAVY_MODEL` читается из окружения без валидации — любое значение кроме `"true"` трактуется как false |

### STRIDE Threat Register

| Threat ID | Категория | Компонент | Решение | Меры |
|-----------|-----------|-----------|---------|------|
| T-1-01 | Tampering | `os.environ["HEAVY_MODEL"]` | accept | Env var задаётся администратором в `.env`, не пользователем. Значение используется только для выбора модели — не для авторизации или данных. Риск минимален. |
| T-1-02 | Tampering | `tests/test_agents.py` — установка env var в тестах | mitigate | Использовать `try/finally` в тестах с `HEAVY_MODEL=true` для гарантии очистки env var, чтобы не загрязнять окружение других тестов. Это уже предусмотрено в Task 3. |
</threat_model>

<output>
После завершения всех задач создать `.planning/phases/01-ispravlenie-baga/01-01-SUMMARY.md` с кратким описанием:
- Что изменено (какие файлы, какие строки)
- Результаты запуска тестов (количество passed/failed)
- Любые отклонения от плана
</output>
```
