---
phase: 2
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestrator/extractor.py
  - orchestrator/pipeline.py
  - prompts/devops.md
  - tests/test_extractor.py
  - tests/test_pipeline.py
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
  - DEPLOY-01
  - DEPLOY-02
must_haves:
  truths:
    - "orchestrator/extractor.py существует и экспортирует функцию extract_files(content: str, base_dir: Path) -> list[Path]"
    - "extract_files парсит code-блоки с маркером '# path: <путь>' в первой строке блока"
    - "Вложенные пути (например .github/workflows/ci.yml) создаются через mkdir(parents=True, exist_ok=True)"
    - "save_results() в pipeline.py вызывает extract_files для code_artifact, test_report и deploy_config"
    - "prompts/devops.md содержит # path: маркеры для Dockerfile, docker-compose.yml, .github/workflows/ci.yml, .env.example, README.md"
    - "pytest tests/test_extractor.py -v проходит без ошибок"
    - "pytest tests/ -v проходит без ошибок (все тесты)"
  artifacts:
    - path: "orchestrator/extractor.py"
      provides: "Парсер # path: маркеров — извлекает реальные файлы из markdown-вывода агентов"
      contains: "def extract_files"
    - path: "tests/test_extractor.py"
      provides: "Unit-тесты для extractor.py"
      contains: "def test_extract_files"
    - path: "orchestrator/pipeline.py"
      provides: "Обновлённый save_results() с вызовом extract_files"
      contains: "extract_files"
    - path: "prompts/devops.md"
      provides: "Промпт DevOps с # path: маркерами для всех конфигов"
      contains: "# path: Dockerfile"
  key_links:
    - from: "orchestrator/pipeline.py:save_results"
      to: "orchestrator/extractor.py:extract_files"
      via: "вызов extract_files(content, project_dir) после сохранения каждого markdown-файла"
    - from: "prompts/devops.md"
      to: "orchestrator/extractor.py:extract_files"
      via: "маркер # path: в первой строке code-блока — именно его парсит extractor"
---

# Plan 2-1: Парсер файлов из markdown-вывода агентов

<objective>
Реализовать автоматическое извлечение реальных файлов проекта из markdown-вывода агентов Dev, QA и DevOps. Создать `orchestrator/extractor.py` с функцией `extract_files()`, которая находит code-блоки с маркером `# path: <путь>` и создаёт файлы на диске. Подключить экстрактор в `save_results()`. Обновить промпт DevOps, чтобы он тоже выводил файлы через маркеры.
</objective>

<tasks>

## Task 1: Создать orchestrator/extractor.py

**Wave:** 1 (независимо от других задач)
**Type:** create
**Files:** `orchestrator/extractor.py`

**Read first:**
- `orchestrator/pipeline.py` — строки 52-72: паттерн сохранения файлов (`project_dir.mkdir`, `.write_text`), чтобы использовать тот же стиль
- `prompts/dev.md` — строки 12-15: формат маркера `# path: src/main.py` внутри code-блока — это то, что нужно распарсить

**Action:**

Создать файл `orchestrator/extractor.py` со следующей логикой:

```python
from __future__ import annotations

import re
from pathlib import Path

_BLOCK_RE = re.compile(r"```[^\n]*\n# path: ([^\n]+)\n(.*?)```", re.DOTALL)


def extract_files(content: str, base_dir: Path) -> list[Path]:
    """Парсит code-блоки с маркером # path: и создаёт файлы на диске.

    Формат маркера (первая строка внутри блока):
        # path: src/main.py

    Возвращает список путей созданных файлов.
    """
    created: list[Path] = []
    for match in _BLOCK_RE.finditer(content):
        rel_path = match.group(1).strip()
        file_content = match.group(2)
        target = base_dir / rel_path
        target.parent.mkdir(parents=True, exist_ok=True)
        target.write_text(file_content, encoding="utf-8")
        created.append(target)
    return created
```

Ничего лишнего — только одна функция с одной ответственностью.

**Acceptance criteria:**
- [ ] Файл `orchestrator/extractor.py` создан
- [ ] Экспортирует `extract_files(content: str, base_dir: Path) -> list[Path]`
- [ ] Использует `re.compile` с `re.DOTALL` для многострочных блоков
- [ ] Путь внутри маркера обрабатывается через `.strip()` (убирает пробелы/переводы строк)
- [ ] Директории создаются через `target.parent.mkdir(parents=True, exist_ok=True)`
- [ ] Содержимое пишется через `.write_text(file_content, encoding="utf-8")`

---

## Task 2: Написать tests/test_extractor.py

**Wave:** 1 (параллельно с Task 1)
**Type:** create
**Files:** `tests/test_extractor.py`

**Read first:**
- `tests/test_pipeline.py` — структура тестов: как используется `tmp_path`, как импортируются модули

**Action:**

Создать `tests/test_extractor.py` с unit-тестами:

```python
"""Unit-тесты для orchestrator/extractor.py."""
from __future__ import annotations

import sys
from pathlib import Path

import pytest

sys.path.insert(0, str(Path(__file__).parent.parent))

from orchestrator.extractor import extract_files


SIMPLE_CONTENT = '''\
Вот код приложения:

```python
# path: src/main.py
print("hello")
```
'''

NESTED_PATH_CONTENT = '''\
CI конфиг:

```yaml
# path: .github/workflows/ci.yml
name: CI
on: [push]
```
'''

MULTI_FILE_CONTENT = '''\
```python
# path: app.py
x = 1
```

```python
# path: tests/test_app.py
assert True
```
'''

NO_MARKER_CONTENT = '''\
```python
print("no marker here")
```
'''


def test_extract_single_file(tmp_path):
    """Базовый случай: один файл с маркером."""
    created = extract_files(SIMPLE_CONTENT, tmp_path)
    assert len(created) == 1
    assert (tmp_path / "src" / "main.py").exists()
    assert (tmp_path / "src" / "main.py").read_text(encoding="utf-8") == 'print("hello")\n'


def test_extract_nested_path(tmp_path):
    """Вложенные директории создаются автоматически."""
    created = extract_files(NESTED_PATH_CONTENT, tmp_path)
    assert len(created) == 1
    ci_file = tmp_path / ".github" / "workflows" / "ci.yml"
    assert ci_file.exists()


def test_extract_multiple_files(tmp_path):
    """Несколько файлов в одном вводе."""
    created = extract_files(MULTI_FILE_CONTENT, tmp_path)
    assert len(created) == 2
    assert (tmp_path / "app.py").exists()
    assert (tmp_path / "tests" / "test_app.py").exists()


def test_no_markers_returns_empty(tmp_path):
    """Если маркеров нет — возвращает пустой список, файлы не создаются."""
    created = extract_files(NO_MARKER_CONTENT, tmp_path)
    assert created == []


def test_empty_content(tmp_path):
    """Пустая строка — возвращает пустой список."""
    created = extract_files("", tmp_path)
    assert created == []


def test_dry_run_content_not_extracted(tmp_path):
    """Dry-run вывод '[dry-run] Dev output' не содержит маркеров — пустой результат."""
    created = extract_files("[dry-run] Dev output", tmp_path)
    assert created == []
```

**Acceptance criteria:**
- [ ] Файл `tests/test_extractor.py` создан с 6 тестами
- [ ] Тест `test_extract_single_file` проверяет содержимое файла (не только факт создания)
- [ ] Тест `test_extract_nested_path` проверяет создание `.github/workflows/ci.yml`
- [ ] Тест `test_no_markers_returns_empty` проверяет, что без маркеров файлы не создаются
- [ ] Тест `test_dry_run_content_not_extracted` явно проверяет dry-run поведение
- [ ] `pytest tests/test_extractor.py -v` — 6 passed, 0 failed

---

## Task 3: Обновить prompts/devops.md — добавить # path: маркеры

**Wave:** 1 (параллельно с Task 1 и 2)
**Type:** edit
**Files:** `prompts/devops.md`

**Read first:**
- `prompts/devops.md` — весь файл: текущая структура без маркеров
- `prompts/dev.md` — строки 12-15: образец формата маркера `# path: src/main.py` внутри code-блока
- `orchestrator/extractor.py` решение (D-01): единый формат `# path: <путь>` для всех агентов

**Action:**

Обновить раздел "Что ты должен произвести" в `prompts/devops.md`, добавив к каждому файлу явный code-блок с маркером `# path:`. Нужно добавить 5 маркеров согласно D-03 и D-04:

- `# path: Dockerfile` — для секции Dockerfile
- `# path: docker-compose.yml` — для секции Docker Compose
- `# path: .github/workflows/ci.yml` — для CI/CD Pipeline
- `# path: .env.example` — для переменных окружения
- `# path: README.md` — для health check + инструкции по деплою (D-04)

Структура каждой секции после обновления:

```
### 1. Dockerfile
Напиши оптимизированный Dockerfile:
- ...

```dockerfile
# path: Dockerfile
FROM python:3.12-slim
...
```
```

Prose-разделы (health check, инструкция по деплою — секции 5 и 6) объединяются в README.md с маркером `# path: README.md`.

Итоговый файл `prompts/devops.md` должен содержать 5 code-блоков с маркерами `# path:` и понятную инструкцию для агента.

**Acceptance criteria:**
- [ ] В `prompts/devops.md` присутствуют маркеры: `# path: Dockerfile`, `# path: docker-compose.yml`, `# path: .github/workflows/ci.yml`, `# path: .env.example`, `# path: README.md`
- [ ] Каждый маркер находится в первой строке своего code-блока (после ` ``` `)
- [ ] Prose-инструкция для агента сохранена — секции с требованиями к содержимому файлов остаются
- [ ] Файл читабелен и понятен агенту как инструкция

---

## Task 4: Подключить extractor в orchestrator/pipeline.py

**Wave:** 2 (зависит от Task 1)
**Type:** edit
**Files:** `orchestrator/pipeline.py`

**Read first:**
- `orchestrator/pipeline.py` — весь файл: строки 52-72 (`save_results()`), строки 1-10 (импорты)
- `orchestrator/extractor.py` (после Task 1): сигнатура `extract_files(content: str, base_dir: Path) -> list[Path]`

**Action:**

1. Добавить импорт в начало файла (после существующих импортов):
   ```python
   from orchestrator.extractor import extract_files
   ```

2. В функции `save_results()`, после строки 69 (цикл сохранения markdown-файлов):
   ```python
   for filename, content in artifacts.items():
       (project_dir / filename).write_text(content, encoding="utf-8")
   ```
   
   Добавить вызовы extractor для трёх артефактов, которые могут содержать маркеры:
   ```python
   # Извлекаем реальные файлы из вывода Dev, QA и DevOps
   extract_files(state.code_artifact.content, project_dir)
   extract_files(state.test_report.content, project_dir)
   extract_files(state.deploy_config.content, project_dir)
   ```

3. Обновить console.print в конце `save_results()` — вместо текущего:
   ```python
   console.print(f"\n[bold green]OK Результаты сохранены:[/] {project_dir}")
   ```
   
   Добавить информацию о реальных файлах:
   ```python
   real_files = [f for f in project_dir.rglob("*") if f.is_file() and not f.name.endswith(".md")]
   console.print(f"\n[bold green]OK Результаты сохранены:[/] {project_dir}")
   if real_files:
       console.print(f"[dim]  Создано файлов проекта: {len(real_files)}[/]")
   ```

**Acceptance criteria:**
- [ ] `from orchestrator.extractor import extract_files` добавлен в импорты
- [ ] В `save_results()` после цикла сохранения markdown присутствуют 3 вызова `extract_files()`
- [ ] Вызовы: для `state.code_artifact.content`, `state.test_report.content`, `state.deploy_config.content`
- [ ] Dry-run не ломается — `"[dry-run] Dev output"` не содержит маркеров → `extract_files()` возвращает `[]`
- [ ] `pytest tests/test_pipeline.py -v` — все 3 теста проходят (0 failed)

---

## Task 5: Добавить интеграционный тест извлечения файлов в test_pipeline.py

**Wave:** 2 (зависит от Task 1 и 4)
**Type:** edit
**Files:** `tests/test_pipeline.py`

**Read first:**
- `tests/test_pipeline.py` — весь файл: существующие 3 теста и их паттерны
- `orchestrator/extractor.py` (после Task 1): понять формат маркера

**Action:**

Добавить в конец `tests/test_pipeline.py` один тест, который проверяет реальную интеграцию extractor + pipeline через `save_results()` с содержимым, содержащим маркеры:

```python
def test_save_results_extracts_real_files(tmp_path):
    """Если code_artifact содержит # path: маркеры, файлы создаются на диске."""
    from orchestrator.state import ProjectState, CodeArtifact

    state = ProjectState(raw_task="Test")
    state.code_artifact = CodeArtifact(content=(
        "```python\n"
        "# path: src/app.py\n"
        "print('hello')\n"
        "```\n"
    ))

    project_dir = save_results(state, output_dir=str(tmp_path))

    # markdown-файл всегда создаётся
    assert (project_dir / "04_code.md").exists()
    # реальный файл тоже создан
    assert (project_dir / "src" / "app.py").exists()
```

**Acceptance criteria:**
- [ ] Тест `test_save_results_extracts_real_files` добавлен в `tests/test_pipeline.py`
- [ ] Тест проверяет, что `04_code.md` создаётся (markdown остаётся)
- [ ] Тест проверяет, что `src/app.py` создаётся (реальный файл извлечён)
- [ ] `pytest tests/test_pipeline.py -v` — 4 теста, 0 failed

</tasks>

<verification>
## Проверочные шаги

1. Проверить наличие extractor:
   ```
   python -c "from orchestrator.extractor import extract_files; print('OK')"
   ```
   Должно вывести `OK` без ошибок.

2. Проверить маркеры в prompts/devops.md:
   ```
   grep "# path:" prompts/devops.md
   ```
   Должно показать 5 строк: Dockerfile, docker-compose.yml, .github/workflows/ci.yml, .env.example, README.md

3. Запустить unit-тесты extractor:
   ```
   pytest tests/test_extractor.py -v
   ```
   Ожидаемый результат: 6 passed, 0 failed.

4. Запустить все тесты:
   ```
   pytest tests/ -v
   ```
   Ожидаемый результат: все тесты passed, 0 failed.

5. Дымовой тест интеграции (вручную):
   ```python
   from pathlib import Path
   from orchestrator.extractor import extract_files
   content = "```python\n# path: hello/world.py\nprint('hi')\n```\n"
   files = extract_files(content, Path("/tmp/test_extract"))
   print(files)  # [PosixPath('/tmp/test_extract/hello/world.py')]
   ```
</verification>

<success_criteria>
## Критерии успеха (must_haves)

- [ ] `orchestrator/extractor.py` создан с функцией `extract_files(content, base_dir)`
- [ ] Вложенные пути (`.github/workflows/ci.yml`) создаются автоматически через `mkdir(parents=True)`
- [ ] `save_results()` в `pipeline.py` вызывает `extract_files` для code, tests и deploy вывода
- [ ] `prompts/devops.md` содержит 5 `# path:` маркеров (Dockerfile, docker-compose.yml, ci.yml, .env.example, README.md)
- [ ] Dry-run режим продолжает работать корректно — `"[dry-run] ... output"` не создаёт лишних файлов
- [ ] `pytest tests/test_extractor.py -v` — 6 passed, 0 failed
- [ ] `pytest tests/ -v` — все тесты passed, 0 failed
</success_criteria>

<threat_model>
## Модель угроз

**Область:** Этап 2 — парсинг строк из вывода LLM и запись файлов на диск

### Доверительные границы

| Граница | Описание |
|---------|----------|
| LLM вывод → парсер | Содержимое `content` генерируется DeepSeek — теоретически может содержать произвольные пути |
| Парсер → файловая система | `extract_files` создаёт файлы в `base_dir` — нужно убедиться, что не выходит за её пределы |

### Реестр угроз STRIDE

| ID | Категория | Компонент | Решение | Меры |
|----|-----------|-----------|---------|------|
| T-2-01 | Tampering | `# path: ../../../etc/passwd` в выводе LLM | mitigate | В рамках этапа 2 путь не нормализуется — LLM доверяем (DeepSeek API, не пользовательский ввод). Это риск для v2. Зафиксировать в backlog как REL-04 при необходимости. |
| T-2-02 | Tampering | Перезапись существующих файлов проекта | accept | `project_dir` уникален по `slug` от задачи. Файлы внутри `project_dir` — наш вывод, перезапись не страшна. |
| T-2-03 | Denial of Service | Огромный вывод LLM с тысячами блоков | accept | DeepSeek API ограничивает длину ответа. Риск реален только при злонамеренных промптах — вне текущего scope. |
</threat_model>

<output>
После завершения всех задач создать `.planning/phases/02-izvlechenie-fajlov/02-01-SUMMARY.md` с кратким описанием:
- Что изменено (какие файлы, какие строки)
- Результаты запуска тестов (количество passed/failed)
- Любые отклонения от плана
</output>
