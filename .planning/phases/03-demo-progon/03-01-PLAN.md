---
phase: 03-demo-progon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/dev.md
  - prompts/qa.md
autonomous: true
requirements:
  - DEMO-02
  - DEMO-03
  - DEMO-04

must_haves:
  truths:
    - "prompts/dev.md содержит явное требование генерировать # path: requirements.txt с полным списком зависимостей"
    - "prompts/dev.md содержит явное требование генерировать # path: README.md с инструкцией docker build + docker run"
    - "prompts/dev.md содержит явное требование читать arch_decision и следовать выбранному стеку"
    - "prompts/dev.md содержит требование добавить эндпоинт GET /health возвращающий {\"status\": \"ok\"}"
    - "prompts/qa.md содержит явное требование использовать pytest + FastAPI TestClient"
    - "prompts/qa.md содержит запрет на httpx с живым сервером и запрет на requests к localhost"
    - "prompts/qa.md содержит пример импорта TestClient и паттерн from main import app"
  artifacts:
    - path: "prompts/dev.md"
      provides: "Промпт Dev-агента с инструкциями по requirements.txt, README.md, /health, следованию arch_decision"
    - path: "prompts/qa.md"
      provides: "Промпт QA-агента с инструкцией TestClient + pytest, без живого сервера"
  key_links:
    - from: "prompts/dev.md"
      to: "orchestrator/extractor.py"
      via: "# path: маркеры в генерируемом коде"
      pattern: "# path: requirements\\.txt"
    - from: "prompts/qa.md"
      to: "orchestrator/extractor.py"
      via: "# path: маркеры в тестах"
      pattern: "from fastapi.testclient import TestClient"
---

<objective>
Улучшить промпты Dev-агента и QA-агента так, чтобы генерируемый ими код был запускаемым внутри Docker без ручных правок.

Purpose: Без requirements.txt Docker-образ не соберётся. Без TestClient тесты не пройдут внутри контейнера (нет живого сервера). Без /health verify.py не сможет подтвердить запуск. Эти промпты — главный рычаг качества генерации.

Output: Обновлённые prompts/dev.md и prompts/qa.md с конкретными, обязательными инструкциями.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Текущие промпты — читать целиком перед правкой -->
@prompts/dev.md
@prompts/qa.md

<!-- Extractor: формат # path: маркеров — промпты должны этот формат соблюдать -->
@orchestrator/extractor.py
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Улучшить prompts/dev.md — requirements.txt, README.md, /health, arch_decision</name>
  <files>prompts/dev.md</files>
  <read_first>
    - prompts/dev.md — текущее содержимое, чтобы добавить секции, не потеряв существующие инструкции
    - orchestrator/extractor.py — паттерн парсинга # path: маркеров (regex: ```[^\n]*\n# path: ([^\n]+)\n)
  </read_first>
  <action>
Прочитать prompts/dev.md полностью. Добавить в файл следующие обязательные секции и инструкции:

**1. Добавить секцию "Стек и архитектура" в начало (после заголовка "# Developer"):**

```
## Стек и архитектура

Ты ВСЕГДА читаешь `arch_decision` (вывод Architect-агента) перед написанием кода.
Стек, ORM, база данных и структура проекта определены Architect-агентом — строго следуй этим решениям.
Не изобретай стек самостоятельно.
```

**2. В секции "Что ты должен произвести" заменить пункт "2. Зависимости" на:**

```
### 2. Файл зависимостей (ОБЯЗАТЕЛЬНО)

ВСЕГДА генерируй `requirements.txt` как отдельный файл с маркером `# path:`:

```python
# path: requirements.txt
fastapi
uvicorn[standard]
sqlalchemy
# ... все зависимости проекта
```

Включай ВСЕ зависимости: фреймворк, ORM, БД-драйвер, валидацию и т.д.
Если проект использует SQLite — явно добавь pydantic, если postgresql — psycopg2-binary.
```

**3. Добавить новый обязательный пункт "3. README с инструкцией Docker" после зависимостей:**

```
### 3. README с инструкцией Docker (ОБЯЗАТЕЛЬНО)

ВСЕГДА генерируй `README.md` с инструкцией запуска через Docker:

```markdown
# path: README.md
## Запуск через Docker

```bash
docker build -t app .
docker run -d -p 8000:8000 app
```

## Локальный запуск

```bash
pip install -r requirements.txt
uvicorn main:app --reload
```
```

**4. Добавить новый обязательный пункт "4. Health-check эндпоинт" перед разделом "Принципы кода":**

```
### 4. Health-check эндпоинт (ОБЯЗАТЕЛЬНО)

ВСЕГДА добавляй в основной файл приложения эндпоинт:

```python
@app.get("/health")
def health():
    return {"status": "ok"}
```

Этот эндпоинт нужен для автоматической проверки работоспособности сервиса.
```

**5. В разделе "Принципы кода" добавить в начало списка:**
- Следуй стеку, выбранному Architect-агентом в `arch_decision` — не переключай БД, ORM или фреймворк

**6. Сохранить существующие разделы** "1. Полный рабочий код", "Принципы кода", "Важно" без изменений (только добавить к принципам пункт выше).

Перенумеровать пункты "Конфигурация" → "5. Конфигурация".
  </action>
  <verify>
    <automated>
      python -c "
content = open('prompts/dev.md', encoding='utf-8').read()
checks = [
    ('arch_decision', 'упоминание arch_decision'),
    ('# path: requirements.txt', 'маркер requirements.txt'),
    ('# path: README.md', 'маркер README.md'),
    ('/health', 'эндпоинт /health'),
    ('status.*ok', 'возврат status ok'),
    ('docker build', 'инструкция docker build'),
    ('docker run', 'инструкция docker run'),
]
import re, sys
failed = []
for pattern, desc in checks:
    if not re.search(pattern, content):
        failed.append(f'MISSING: {desc} (pattern: {pattern})')
if failed:
    print('FAIL')
    for f in failed: print(' ', f)
    sys.exit(1)
else:
    print('PASS — все проверки прошли')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "arch_decision" prompts/dev.md` возвращает >= 1
    - `grep -c "# path: requirements.txt" prompts/dev.md` возвращает >= 1
    - `grep -c "# path: README.md" prompts/dev.md` возвращает >= 1
    - `grep -c "/health" prompts/dev.md` возвращает >= 1
    - `grep -c "docker build" prompts/dev.md` возвращает >= 1
    - `grep -c "docker run" prompts/dev.md` возвращает >= 1
    - `grep -c "status.*ok" prompts/dev.md` возвращает >= 1
  </acceptance_criteria>
  <done>prompts/dev.md содержит обязательные инструкции: arch_decision, requirements.txt, README.md, /health эндпоинт с docker-инструкцией</done>
</task>

<task type="auto">
  <name>Задача 2: Улучшить prompts/qa.md — TestClient + pytest, без живого сервера</name>
  <files>prompts/qa.md</files>
  <read_first>
    - prompts/qa.md — текущее содержимое, чтобы сохранить структуру и дополнить нужными инструкциями
    - orchestrator/extractor.py — формат # path: маркеров для тестовых файлов
  </read_first>
  <action>
Прочитать prompts/qa.md полностью. Внести следующие изменения:

**1. В раздел "2. Автоматизированные тесты" добавить обязательные требования к фреймворку тестирования ПЕРЕД примером блока кода:**

```
#### Фреймворк тестирования (ОБЯЗАТЕЛЬНО)

Используй ТОЛЬКО `pytest` + `fastapi.testclient.TestClient`.

НЕЛЬЗЯ:
- `httpx.AsyncClient` с `base_url="http://localhost:..."` (требует живой сервер)
- `requests.get("http://localhost:8000/...")` (требует живой сервер)
- Любые обращения к `localhost` или `127.0.0.1` в тестах

ПРАВИЛЬНЫЙ импорт и паттерн:

```python
# path: tests/test_api.py
import pytest
from fastapi.testclient import TestClient
from main import app  # импортируй app из основного файла

client = TestClient(app)


def test_health():
    response = client.get("/health")
    assert response.status_code == 200
    assert response.json() == {"status": "ok"}
```

TestClient создаёт встроенный тестовый транспорт — не нужен запущенный сервер.
Работает внутри Docker-контейнера без дополнительных настроек.
```

**2. В разделе "Принципы" добавить в конец:**
- Используй pytest + TestClient — не обращайся к localhost в тестах, это не работает внутри Docker

**3. Сохранить существующие разделы** "1. Тест-план", "3. Баг-репорты", "4. Чеклист качества" без изменений.
  </action>
  <verify>
    <automated>
      python -c "
content = open('prompts/qa.md', encoding='utf-8').read()
checks = [
    ('TestClient', 'упоминание TestClient'),
    ('from fastapi.testclient import TestClient', 'импорт TestClient'),
    ('from main import app', 'импорт app'),
    ('pytest', 'упоминание pytest'),
    ('localhost', 'запрет localhost — должно быть упомянуто как запрет'),
    ('НЕЛЬЗЯ|нельзя|запрет|не.*localhost', 'явный запрет'),
]
import re, sys
failed = []
# TestClient и pytest должны быть
for pattern, desc in checks[:5]:
    if not re.search(pattern, content):
        failed.append(f'MISSING: {desc}')
# Проверяем что localhost упомянут как запрет (контекст 'нельзя' рядом)
if 'localhost' not in content:
    failed.append('MISSING: упоминание localhost как запрещённого паттерна')
if failed:
    print('FAIL')
    for f in failed: print(' ', f)
    sys.exit(1)
else:
    print('PASS — все проверки прошли')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TestClient" prompts/qa.md` возвращает >= 2 (импорт + использование)
    - `grep -c "from fastapi.testclient import TestClient" prompts/qa.md` возвращает >= 1
    - `grep -c "from main import app" prompts/qa.md` возвращает >= 1
    - `grep -c "pytest" prompts/qa.md` возвращает >= 2
    - `grep -c "localhost" prompts/qa.md` возвращает >= 1 (упомянут как запрещённый паттерн)
    - `grep -i "НЕЛЬЗЯ\|нельзя\|запрет" prompts/qa.md` возвращает >= 1
  </acceptance_criteria>
  <done>prompts/qa.md содержит явный запрет на httpx/requests к localhost, обязательный импорт TestClient, рабочий пример теста с from main import app</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| промпт → LLM | Инструкции в промпте влияют на поведение LLM, но не гарантируют точное следование |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | prompts/dev.md | accept | Промпты — конфигурация, не исполняемый код; изменения видны в git diff |
| T-03-02 | Repudiation | prompts/qa.md | accept | Изменения фиксируются в git с коммитом этапа |
</threat_model>

<verification>
После выполнения обоих задач запустить python main.py --dry-run "тест" и убедиться что pipeline запускается без ошибок (промпты корректно загружаются агентами).
</verification>

<success_criteria>
- prompts/dev.md содержит: arch_decision, # path: requirements.txt, # path: README.md, /health эндпоинт, docker build + docker run
- prompts/qa.md содержит: TestClient, from fastapi.testclient import TestClient, from main import app, явный запрет localhost
- Оба промпта загружаются без синтаксических ошибок
</success_criteria>

<output>
После завершения создать .planning/phases/03-demo-progon/03-01-SUMMARY.md
</output>
