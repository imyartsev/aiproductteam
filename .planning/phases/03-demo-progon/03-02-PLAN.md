---
phase: 03-demo-progon
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - verify.py
autonomous: true
requirements:
  - DEMO-03
  - DEMO-04

must_haves:
  truths:
    - "verify.py существует в корне проекта рядом с main.py"
    - "verify.py принимает путь к папке проекта как аргумент командной строки"
    - "verify.py выполняет docker build и выводит [OK] или [FAIL] с деталями"
    - "verify.py запускает docker run -d и ждёт старта контейнера с retry-loop"
    - "verify.py проверяет GET http://localhost:8000/health через requests"
    - "verify.py запускает pytest внутри Docker и проверяет exit code 0"
    - "verify.py выводит итоговое PASS или FAIL"
    - "verify.py останавливает и удаляет Docker-контейнер после проверки"
  artifacts:
    - path: "verify.py"
      provides: "Скрипт верификации сгенерированного проекта через Docker"
      exports: ["main entry point: python verify.py projects/<slug>"]
  key_links:
    - from: "verify.py"
      to: "docker build"
      via: "subprocess.run(['docker', 'build', ...])"
      pattern: "subprocess.*docker.*build"
    - from: "verify.py"
      to: "http://localhost:8000/health"
      via: "requests.get с retry-loop"
      pattern: "requests\\.get.*health"
---

<objective>
Создать скрипт verify.py в корне проекта, который верифицирует сгенерированный проект через Docker: сборку образа, запуск сервера, health-check, прогон тестов.

Purpose: verify.py — единственный объективный критерий успеха демо-прогона (D-05). Без него нельзя автоматически проверить что сгенерированный код реально запускается.

Output: Рабочий verify.py с интерфейсом: python verify.py projects/<slug>, выводящий пошаговый лог и итоговый PASS/FAIL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-demo-progon/03-CONTEXT.md

<!-- Структура проекта — понять куда кладём verify.py -->
@orchestrator/pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Создать verify.py — четыре шага верификации через Docker</name>
  <files>verify.py</files>
  <read_first>
    - .planning/phases/03-demo-progon/03-CONTEXT.md — D-03, D-04: точный интерфейс и шаги (обязательное чтение)
    - orchestrator/pipeline.py — паттерн slug: join(c if c.isalnum() or c in "-_" else "_") — verify.py должен извлекать slug из имени папки
  </read_first>
  <behavior>
    - Аргумент командной строки: путь к папке проекта (например, projects/Создай_FastAPI_сервис)
    - Шаг 1 docker build: subprocess.run(['docker', 'build', '-t', slug, str(project_dir)]) → [OK] или [FAIL] с stderr
    - Шаг 2 docker run: subprocess.run(['docker', 'run', '-d', '-p', '8000:8000', '--name', slug, slug]) → retry-loop до 15 секунд (3 попытки по 5 сек), [OK] или [FAIL]
    - Шаг 3 health check: requests.get('http://localhost:8000/health') с timeout=5 → [OK] если status_code == 200, [FAIL] с деталями иначе
    - Шаг 4 pytest: subprocess.run(['docker', 'run', '--rm', slug, 'pytest']) → [OK] если returncode == 0, [FAIL] с stdout/stderr
    - Финал: PASS (все шаги OK) или FAIL (первый провалившийся шаг останавливает выполнение)
    - Cleanup: docker stop + docker rm в блоке finally (всегда, даже при FAIL)
    - Выход: sys.exit(0) при PASS, sys.exit(1) при FAIL
  </behavior>
  <action>
Создать файл verify.py в корне проекта со следующей структурой:

```python
#!/usr/bin/env python3
"""Верификатор сгенерированного проекта через Docker.

Использование:
    python verify.py projects/<slug>

Шаги:
    1. docker build -t <slug> <project_dir>
    2. docker run -d -p 8000:8000 --name <slug> <slug>  +  ожидание старта
    3. GET http://localhost:8000/health
    4. docker run --rm <slug> pytest

Выводит PASS или FAIL с деталями каждого шага.
"""

from __future__ import annotations

import sys
import time
import subprocess
from pathlib import Path

import requests


def run_cmd(cmd: list[str], **kwargs) -> subprocess.CompletedProcess:
    """Запускает команду и возвращает результат."""
    return subprocess.run(cmd, capture_output=True, text=True, **kwargs)


def step_ok(label: str) -> None:
    print(f"[OK] {label}")


def step_fail(label: str, details: str = "") -> None:
    print(f"[FAIL] {label}")
    if details:
        print(f"       {details.strip()}")


def verify(project_dir: Path) -> bool:
    slug = project_dir.name
    container_name = slug

    # --- Шаг 1: docker build ---
    result = run_cmd(["docker", "build", "-t", slug, str(project_dir)])
    if result.returncode != 0:
        step_fail("docker build", result.stderr or result.stdout)
        return False
    step_ok(f"docker build -t {slug}")

    # --- Шаг 2: docker run + ожидание старта ---
    run_cmd(["docker", "rm", "-f", container_name])  # убрать старый если есть
    result = run_cmd([
        "docker", "run", "-d",
        "-p", "8000:8000",
        "--name", container_name,
        slug,
    ])
    if result.returncode != 0:
        step_fail("docker run", result.stderr or result.stdout)
        return False

    # Retry-loop: ждём до 15 секунд (3 попытки по 5 сек)
    started = False
    for attempt in range(1, 4):
        time.sleep(5)
        try:
            resp = requests.get("http://localhost:8000/health", timeout=3)
            if resp.status_code < 500:
                started = True
                break
        except requests.exceptions.ConnectionError:
            pass
        print(f"  (ожидание старта, попытка {attempt}/3...)")

    if not started:
        step_fail("docker run — сервер не запустился за 15 секунд")
        return False
    step_ok("docker run -d -p 8000:8000")

    # --- Шаг 3: GET /health ---
    try:
        resp = requests.get("http://localhost:8000/health", timeout=5)
        if resp.status_code == 200:
            step_ok(f"GET /health → {resp.status_code} {resp.text[:80]}")
        else:
            step_fail(f"GET /health → {resp.status_code}", resp.text[:200])
            return False
    except requests.exceptions.RequestException as exc:
        step_fail("GET /health", str(exc))
        return False

    # --- Шаг 4: pytest внутри Docker ---
    result = run_cmd(["docker", "run", "--rm", slug, "pytest", "-v"])
    if result.returncode == 0:
        step_ok("docker run pytest")
        if result.stdout:
            # Показать последние строки вывода pytest
            lines = result.stdout.strip().splitlines()
            for line in lines[-5:]:
                print(f"  {line}")
    else:
        step_fail("docker run pytest", (result.stdout + result.stderr)[:500])
        return False

    return True


def cleanup(slug: str) -> None:
    """Остановить и удалить контейнер."""
    run_cmd(["docker", "stop", slug])
    run_cmd(["docker", "rm", slug])


def main() -> None:
    if len(sys.argv) != 2:
        print("Использование: python verify.py projects/<slug>")
        sys.exit(1)

    project_dir = Path(sys.argv[1])
    if not project_dir.exists():
        print(f"[FAIL] Папка не найдена: {project_dir}")
        sys.exit(1)

    slug = project_dir.name
    print(f"\n=== Верификация проекта: {slug} ===\n")

    passed = False
    try:
        passed = verify(project_dir)
    finally:
        cleanup(slug)

    print()
    if passed:
        print("PASS")
        sys.exit(0)
    else:
        print("FAIL")
        sys.exit(1)


if __name__ == "__main__":
    main()
```

Важные детали реализации:
- `slug = project_dir.name` — используем имя папки как имя Docker-образа и контейнера
- retry-loop: 3 попытки по 5 секунд = 15 секунд максимум (per Claude's Discretion из CONTEXT.md)
- Шаг 4 использует `docker run --rm` (не запущенный контейнер) — запускает pytest внутри нового контейнера от того же образа
- `cleanup()` вызывается в `finally` — всегда, даже при FAIL
- `capture_output=True` в run_cmd — не засоряем stdout при нормальной работе
  </action>
  <verify>
    <automated>
      python -c "
import ast, sys
with open('verify.py', encoding='utf-8') as f:
    source = f.read()

# Синтаксическая проверка
try:
    ast.parse(source)
    print('[OK] синтаксис Python корректен')
except SyntaxError as e:
    print(f'[FAIL] синтаксическая ошибка: {e}')
    sys.exit(1)

# Проверка ключевых паттернов
import re
checks = [
    (r'import requests', 'import requests'),
    (r'import subprocess', 'import subprocess'),
    (r'docker.*build', 'docker build команда'),
    (r'docker.*run.*-d', 'docker run -d'),
    (r'localhost:8000/health', 'health check URL'),
    (r'docker.*run.*pytest', 'pytest в Docker'),
    (r'docker.*stop', 'docker stop cleanup'),
    (r'docker.*rm', 'docker rm cleanup'),
    (r'sys\.exit\(0\)', 'sys.exit(0) при PASS'),
    (r'sys\.exit\(1\)', 'sys.exit(1) при FAIL'),
    (r'time\.sleep', 'retry sleep'),
    (r'finally', 'finally блок для cleanup'),
]
failed = []
for pattern, desc in checks:
    if not re.search(pattern, source):
        failed.append(f'MISSING: {desc}')
if failed:
    print('FAIL')
    for f in failed: print(' ', f)
    sys.exit(1)
else:
    print('PASS — все проверки прошли')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Файл `verify.py` существует в корне проекта (рядом с main.py)
    - `python verify.py` (без аргументов) выводит "Использование: python verify.py projects/<slug>" и завершается с exit code 1
    - `python -c "import ast; ast.parse(open('verify.py').read()); print('OK')"` выводит OK (синтаксически корректен)
    - `grep -c "docker build" verify.py` >= 1
    - `grep -c "docker run" verify.py` >= 2 (запуск сервера + pytest)
    - `grep -c "localhost:8000/health" verify.py` >= 1
    - `grep -c "finally" verify.py` >= 1 (cleanup в finally)
    - `grep -c "sys.exit" verify.py` >= 2 (0 и 1)
    - `grep -c "time.sleep" verify.py` >= 1 (retry-loop)
  </acceptance_criteria>
  <done>verify.py создан, синтаксически корректен, содержит все 4 шага верификации, retry-loop, cleanup в finally, PASS/FAIL вывод</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI аргумент → subprocess | Путь к папке передаётся в docker build — потенциальный path traversal |
| Docker контейнер → localhost | Контейнер имеет доступ к портам хоста |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | verify.py subprocess | accept | Скрипт используется только разработчиком локально, не публичный API; путь проверяется через Path().exists() |
| T-03-04 | Elevation of Privilege | docker run | accept | Docker daemon требует привилегий по дизайну; это developer tooling, не production |
| T-03-05 | Denial of Service | port 8000 занят | mitigate | docker rm -f перед запуском контейнера предотвращает конфликт портов |
</threat_model>

<verification>
Запустить: python verify.py (без аргументов) — должен вывести инструкцию использования и exit 1.
Запустить: python -c "import verify" — должен импортироваться без ошибок.
</verification>

<success_criteria>
- verify.py существует в корне проекта
- Синтаксически корректен (ast.parse проходит)
- Реализованы все 4 шага: docker build, docker run + wait, GET /health, docker run pytest
- Cleanup в finally
- Выводит пошаговый [OK]/[FAIL] лог и итоговый PASS/FAIL
- sys.exit(0) при PASS, sys.exit(1) при FAIL
</success_criteria>

<output>
После завершения создать .planning/phases/03-demo-progon/03-02-SUMMARY.md
</output>
